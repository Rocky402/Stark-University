<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" type="text/css" href="../assets/style.css">

    <title>View-Contact</title>
</head>

<body>

    <%- include('./header.ejs')%>

        <div class="row">
            <div class="col-md-3">
                <%- include('./left.ejs')%>
            </div>
            <div class="col-md-9">


                <h1 class="text-center my-5">View Contact Us</h1>
                <h4 class="text-center">Saved by the latest update in records</h4>

                <div class="container my-5">
                    <% if (viewCon.length>0){ %>
                    <table>
                        <thead>
                        <tr>
                            <th>Applicant's Name</th>
                            <th>Applicant's Email</th>
                            <th>Applicant's Message</th>
                            <th>
                                Delete
                            </th>
                        </tr>

                    </thead>
                        <tbody>
                            <% for(var i = 0;i<viewCon.length;i++){%>
                                <tr>
                                    <td><%= viewCon[i].cname %></td>
                                    <td><%= viewCon[i].cemail %></td>
                                    <td><%= viewCon[i].ctext %></td>
                                    <td>
                                        <a href="/deleteC/<%=viewCon[i].id %>"><button>Delete</button></a></td>
                                </tr>
                                <% } %>
                        </tbody>

                    </table>
                    <% } else { %>
                    No Record Found
                <%} %>
                </div>
            </div>
</body>